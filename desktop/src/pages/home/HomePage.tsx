/**
 * pages/home/HomePage.tsx — STUB mount point for the Home screen.
 * Owned by feature task `feat-home-import`. Fill this directory only.
 */
import { StubPage } from "../../components/StubPage";

export default function HomePage() {
  return <StubPage name="Home" owner="feat-home-import" glyph="🏠" />;
}
