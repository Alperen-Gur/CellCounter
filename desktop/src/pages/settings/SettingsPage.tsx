/**
 * pages/settings/SettingsPage.tsx — STUB mount point for the Settings screen.
 * Owned by feature task `feat-settings`. Fill this directory only.
 */
import { StubPage } from "../../components/StubPage";

export default function SettingsPage() {
  return <StubPage name="Settings" owner="feat-settings" glyph="⚙️" />;
}
