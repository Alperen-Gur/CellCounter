/**
 * pages/onboarding/OnboardingPage.tsx — STUB mount point for Onboarding + Calibration.
 * Owned by feature task `feat-calibration-onboarding`. Fill this directory only.
 */
import { StubPage } from "../../components/StubPage";

export default function OnboardingPage() {
  return (
    <StubPage
      name="Onboarding & Calibration"
      owner="feat-calibration-onboarding"
      glyph="🚀"
    />
  );
}
