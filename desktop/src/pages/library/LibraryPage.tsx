/**
 * pages/library/LibraryPage.tsx — STUB mount point for the Images Library screen.
 * Owned by feature task `feat-library-dedup`. Fill this directory only.
 */
import { StubPage } from "../../components/StubPage";

export default function LibraryPage() {
  return <StubPage name="Library" owner="feat-library-dedup" glyph="🖼️" />;
}
