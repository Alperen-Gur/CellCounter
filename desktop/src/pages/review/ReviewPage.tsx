/**
 * pages/review/ReviewPage.tsx — STUB mount point for the Review Queue screen.
 * Owned by feature task `feat-review-queue`. Fill this directory only.
 */
import { StubPage } from "../../components/StubPage";

export default function ReviewPage() {
  return <StubPage name="Review Queue" owner="feat-review-queue" glyph="✅" />;
}
