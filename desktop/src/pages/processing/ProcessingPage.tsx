/**
 * pages/processing/ProcessingPage.tsx — STUB mount point for the Processing screen.
 * Owned by feature task `feat-processing`. Fill this directory only.
 */
import { StubPage } from "../../components/StubPage";

export default function ProcessingPage() {
  return <StubPage name="Processing" owner="feat-processing" glyph="⏳" />;
}
