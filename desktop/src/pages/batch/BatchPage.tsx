/**
 * pages/batch/BatchPage.tsx — STUB mount point for the Batch screen.
 * Owned by feature task `feat-batch`. Fill this directory only.
 */
import { StubPage } from "../../components/StubPage";

export default function BatchPage() {
  return <StubPage name="Batch" owner="feat-batch" glyph="🗂️" />;
}
