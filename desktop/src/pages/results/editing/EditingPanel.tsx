/**
 * pages/results/editing/EditingPanel.tsx — STUB mount point for the mask-editing UI.
 * Owned by feature task `feat-mask-editing`. This is a SEPARATE directory from
 * pages/results/ so the editing gestures own physically-disjoint files; the
 * Results page composes this panel. Fill pages/results/editing/ only.
 */
import { StubPage } from "../../../components/StubPage";

export default function EditingPanel() {
  return <StubPage name="Mask Editing" owner="feat-mask-editing" glyph="✏️" />;
}
