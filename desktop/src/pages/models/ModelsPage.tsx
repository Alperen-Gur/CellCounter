/**
 * pages/models/ModelsPage.tsx — STUB mount point for the Models screen.
 * Owned by feature task `feat-models`. Fill this directory only.
 */
import { StubPage } from "../../components/StubPage";

export default function ModelsPage() {
  return <StubPage name="Models" owner="feat-models" glyph="🧠" />;
}
