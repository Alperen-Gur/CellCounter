/**
 * pages/compare/ComparePage.tsx — STUB mount point for the Compare screen.
 * Owned by feature task `feat-compare`. Fill this directory only.
 */
import { StubPage } from "../../components/StubPage";

export default function ComparePage() {
  return <StubPage name="Compare" owner="feat-compare" glyph="📊" />;
}
