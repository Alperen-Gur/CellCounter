import { Keyboard, ListChecks, ShieldCheck } from "lucide-react";
export function HelpView({ onShortcuts, onCapabilities, onWelcome }: { onShortcuts(): void; onCapabilities(): void; onWelcome(): void }) {
  return <main className="page-view"><header className="page-heading"><div><span className="eyebrow">CellCounter Web · 0.2.0</span><h1>Help & about</h1><p>Inspect, preview, process, and review microscopy images on this device.</p></div></header><div className="settings-groups"><section><div className="settings-title"><Keyboard size={20} /><div><h2>Using the workbench</h2><p>Preview an image to check your setup. Process saves a result for correction, review, and export.</p></div></div><button className="preference-row" onClick={onWelcome}><span>Welcome & introduction</span><strong>Open</strong></button><button className="preference-row" onClick={onShortcuts}><span>Keyboard shortcuts</span><strong>Open</strong></button><button className="preference-row" onClick={onCapabilities}><span><ListChecks size={17} /> Browser capabilities</span><strong>Open</strong></button></section><section><div className="settings-title"><ShieldCheck size={20} /><div><h2>Your images stay here</h2><p>No image uploads or analytics.</p></div></div><div className="privacy-contract"><div><strong>Saved in this browser on this device</strong><p>Images, jobs, and corrections use local browser storage. Clearing site data removes them. Export the results you need to keep.</p></div></div></section></div></main>;
}
